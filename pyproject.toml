[project]
name = "simeystvo-bot"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot[job-queue]==21.6",
    "python-dotenv==1.0.1",
    "flask==3.1.1",
]

[tool.vercel]
entrypoint = "api.telegram:app"
